# Exact-Candidate Fast-Forward Integration — Independent Audit

| Field | Value |
|---|---|
| Change ID | `exact-candidate-fast-forward-integration` |
| Candidate commit | `7e74813b4eef3f9c07efbd48302830c2ddaf25fd` |
| Candidate tree | `07c41325af112bb107446997e7b474cc0a7b4e40` |
| Result | `GO` |
| Reviewer | `anup19950725` |
| Audited at | `2026-08-29T13:08:46Z` |
| Boundary | Local verified tooling only; hosted and live gates are `NOT_RUN` |

## Decision

A fresh reviewer, distinct from accountable owner `apbusinessidentity-tech` and
implementer `addressanup`, independently inspected the exact verified successor
read-only and found no Critical, High, Medium, or Low candidate finding. The
decision is `GO` for the local verified operator candidate only.

GitHub login `anup19950725` was supplied externally as the accountable reviewer
identity after the independent decision. This record does not claim that an
exact-head GitHub review already exists; that hosted review remains a later gate.

## Binding and scope

- Reset `1e381b5bf0bb024739cdc654d0d5aed5f128aed4` and prior brief-absent
  reset `01992e1f7745ab9eb423e76d5969040b7373be56` have identical tree
  `5eff6de773e26cf3cb126853d158de83fddf3793`.
- Reset-to-proposal changes only the added brief, and the trusted workflow's
  added-file derivation plus Git addition history both resolve exact proposal
  `b22b903cf1adb94c374a5a7225782930e58aa907`.
- The external owner envelope binds `apbusinessidentity-tech`, implementer
  `addressanup`, the exact proposal, and `active-user-interaction` source.
- Implementation `d2d0b1287350c3eb7ed2956ccb6890b80b505279`, tree
  `228083e557f3770480569618f93af0e8143a1c88`, changes only the two
  approved harness scripts and deletes the stale verification record.
- Verification successor `7e74813b...` adds only the fresh PASS record. Its
  recorded candidate/tree, script hashes, ancestry, scope, and artifact budget
  reproduce from Git. No implementation follows verification.

## Control conclusions

The operator and offline contract fail closed on the complete approved boundary:

- exact full repository, pull request, base, head, tree, owner, and auditor input;
- sole-administrator, repository-setting, ruleset, PR, review, ref, tree,
  protection, required-check, and combined-status validation;
- unique greatest trusted-app `started_at`, rejecting missing or tied chronology;
- active-terminal full-SHA confirmation followed by complete authority refresh;
- rejection of initial or post-confirmation automatic source-branch deletion;
- isolated `env -i` Git transport with no ambient configuration or override;
- one exact expected-old lease-bound fast-forward and no alternate mutation API;
- restoration remaining armed until a canonical protection GET proves equality;
- blocking recovery on restoration failure or mismatch; and
- final main/tree equality, source preservation, restored protection, unchanged
  repository contract, and GitHub-reported indirect merge state.

The verification evidence reports repository-pinned `make verify`, the focused
contract with three malformed-input probes, twenty-three failure scenarios and
two ordered successes, ShellCheck, shell syntax, reproducibility, diff hygiene,
and Darwin arm64/amd64 cross-builds passing at the exact implementation.

## Hosted and live boundary

This decision is not hosted readiness and does not authorize the real operator.
Commits `1e381b5b...`, `b22b903...`, `d2d0b128...`, and `7e74813b...` are not
published on GitHub. No protected ref at the corrected reset base, corrected
pull request, candidate hosted check, exact-head owner review, or exact-head
auditor review exists.

Remote `main` remains `be5c0c8f99b8ec55b42e1919533400fa0b41f46c`.
PR #3 remains open, clean, and unmerged at exact candidate `f92c560c...`.
PR #4 remains stale at `83e5b78a...` against the unprotected `a178f047...`
base and supplies no evidence for this successor. All predecessor checks,
reviews, envelopes, and topology decisions are non-transferable.

Before any hosted evaluation, a dedicated base ref must point exactly to
`1e381b5b...`, carry the brief-required protection with administrator
enforcement and no direct/force/delete/bypass ambiguity, and reproduce the
trusted workflow, controller, bootstrap, and toolchain inputs. The exact event
base, brief addition, ref, protection, permissions, checks, and reviews must be
freshly revalidated. Any mismatch fails closed.

## Preservation, rollback, and next boundary

The audit ran read-only. No test, build, controller, operator, provider, Git ref,
GitHub state, installation, release, deployment, signing, or publication action
was performed. Local `main`, PR #1, PR #3, the invalid worktree, historical
commits, and unrelated user state remain unchanged.

Ordinary-revert this audit record to return exactly to verification successor
`7e74813b...`. Revert verification, implementation, and proposal in reverse
order to return to reset tree `5eff6de7...`. Never reset, rebase, force-push,
delete, or rewrite history.

The sole next transition is separately authorized creation and proof of the
protected hosted base at exact reset `1e381b5b...`. Audit `GO`, passing local
tests, or this record cannot publish the candidate, submit reviews, run hosted
checks, or execute the live integration automatically.
