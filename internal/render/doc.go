// Package render decodes, validates, and compiles provider-neutral Level 7
// semantic contracts. Production code in this package is pure: callers supply
// every byte and the package exposes no filesystem, process, network, clock,
// randomness, credential, terminal, or mutation interface.
package render
